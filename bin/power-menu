#!/usr/bin/env bash
set -euo pipefail

choice=$(printf "%s\n" \
  " Lock" \
  "⏾ Standby" \
  "⏻ Shutdown" \
  " Restart" \
  "Cancel" \
  | fuzzel --dmenu -p "Power:")

case "$choice" in
  "⏻ Shutdown") systemctl poweroff ;;
  " Restart")  systemctl reboot ;;
  "⏾ Standby")  systemctl suspend ;;
  " Lock")     launch-lock;;
  *) exit 0 ;;
esac

